<div class="form-toread">
  <%= form_for(@toread_list, :html => {class: "form-horizontal", role: "form"}) do |f| %>
    <% if @toread_list.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@toread_list.errors.count, "error") %> prohibited this toread_list from being saved:</h2>
        <% @toread_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div class="form-group">
    <div class="col-sm-10">
      <label for="Title">Title</label>
      <%= f.text_field :title, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-10">
      <input class="btn btn-default" type="submit" value="Submit">
      <button class="button"><%= link_to"Cancel", toread_lists_path %></button>
    </div>
  </div>
  <% end %>
</div>
